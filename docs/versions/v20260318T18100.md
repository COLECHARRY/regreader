# 版本更新文档

## 版本: 20260318T18100
生成时间: 2026/3/19 02:10:00
分析范围: 自上次分析后的变化

---

### 📥 新增提交 (0)

### ⚠️ 未提交的更改 (5)

**修改文件:**
- ndex.html
- .git-monitor-state.json
- docs/
- git-monitor-config.json
- git-monitor.js

### 🤖 AI 分析

## Analysis

**File Changed: `index.html`**

- **What:** Changed a bit shift operation from `>>2` to `>>4` in the `copyCode` function
- **Why:** This adjusts how the memory offset is calculated. The `>> 4` shift divides the offset by 16 instead of by 4. This suggests the underlying memory mapping or register addressing scheme changed—likely the target hardware now uses a different word/byte alignment, or the software API was updated to match new hardware requirements.

---

**New Files Added:**
- `.git-monitor-state.json` - Tracks last analyzed commit
- `git-monitor-config.json` - Configuration for version documentation
- `git-monitor.js` - A git monitoring script for tracking commits and generating version docs

### 📝 代码差异

```diff
diff --git a/index.html b/index.html
index 48b9bcc..39830fe 100644
--- a/index.html
+++ b/index.html
@@ -832,7 +832,7 @@ async function copyCode(offset) {
 
   const mainHex = val.toString(16).toUpperCase().padStart(8, '0');
   const comment = commentParts.length > 0 ? ` // ${commentParts.join(', ')}` : '';
-  const code = `adi.write(${offset}>>2, 0x${mainHex});${comment}`;
+  const code = `adi.write(${offset}>>4, 0x${mainHex});${comment}`;
   return code;
 }
### New File: .git-monitor-state.json

```
{
  "lastCommitHash": "3ca2993d7c510b76cd3f6b71f821e766a3d3a3fb",
  "lastAnalyzeTime": "2026-03-18T17:54:27.514Z"
}
```

### New File: git-monitor-config.json

```
{
  "versionDocDir": "docs/versions",
  "modelApiKey": "sk-cp-02PcjpCFGR5ItHYDLcdmnsEDd8BUI1dHPJtI0_-cAtUNClIh3Zub2ClriOndaipF3rkVTf14A-ti2nhWTr0vJaKy-DD8xl4z8Fiox7dzdeuSQnx22bXTT9A"
}

```

### New File: git-monitor.js

```
const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');
const https = require('https');

const SCRIPT_DIR = __dirname;
const CONFIG_FILE = path.join(SCRIPT_DIR, 'git-monitor-config.json');
const STATE_FILE = path.join(SCRIPT_DIR, '.git-monitor-state.json');

// 加载配置
let config = {
  modelApiKey: '',
  versionDocDir: null
};

if (fs.existsSync(CONFIG_FILE)) {
  config = { ...config, ...JSON.parse(fs.readFileSync(CONFIG_FILE, 'utf-8')) };
}

// 确保版本目录存在
if (config.versionDocDir && !fs.existsSync(config.versionDocDir)) {
  fs.mkdirSync(config.versionDocDir, { recursive: true });
}

// 加载状态（上次分析的 commit）
let state = {
  lastCommitHash: '',
  lastAnalyzeTime: ''
};

if (fs.existsSync(STATE_FILE)) {
  state = JSON.parse(fs.readFileSync(STATE_FILE, 'utf-8'));
}

// 获取当前仓库的最新 commit
function getLatestCommit(repoPath) {
  try {
    const hash = execSync('git log -1 --format="%H"', { 
      cwd: repoPath, 
      encoding: 'utf-8' 
    }).trim();
    return hash;
  } catch (e) {
    return null;
  }
}

// 获取两次 commit 之间的差异
function getDiffBetweenCommits(repoPath, oldCommit, newCommit) {
  try {
    const diff = execSync(`git diff ${oldCommit}..${newCommit} --unified=3`, { 
      cwd: repoPath, 
      encoding: 'utf-8',
      maxBuffer: 10 * 1024 * 1024
    }).trim();
    return diff;
  } catch (e) {
    return '';
  }
}

// 获取新提交
function getNewCommits(repoPath, afterCommit) {
  try {
    const commits = execSync(`git log ${afterCommit}..HEAD --pretty=format:"%H|||%s|||%ai|||%an"`, { 
      cwd: repoPath, 
      encoding: 'utf-8' 
    }).trim();
    
    if (!commits) return [];
    
    return commits.split('\n').map(c => {
      const parts = c.split('|||');
      return {
        hash: parts[0],
        message: parts[1],
        date: parts[2],
        author: parts[3]
      };
    }).reverse(); // 从旧到新
  } catch (e) {
    return [];
  }
}

// 获取未提交的更改
function getUncommittedChanges(repoPath) {
  try {
    const status = execS
```

```
